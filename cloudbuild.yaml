steps:
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "VITE_GOOGLE_CLIENT_ID=$$GOOGLE_CLIENT_ID" > frontend/.env.production
        docker build -t gcr.io/$PROJECT_ID/spendlens .
    secretEnv: ['GOOGLE_CLIENT_ID']

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/spendlens'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud run deploy spendlens \
          --image=gcr.io/$PROJECT_ID/spendlens \
          --region=asia-southeast1 \
          --platform=managed \
          --allow-unauthenticated \
          --timeout=900 \
          --set-env-vars=GEMINI_API_KEY=$$GEMINI_API_KEY,GOOGLE_CLIENT_ID=$$GOOGLE_CLIENT_ID,GOOGLE_CLIENT_SECRET=$$GOOGLE_CLIENT_SECRET,SESSION_SECRET=$$SESSION_SECRET,CRON_SECRET=$$CRON_SECRET,FIRESTORE_DATABASE_ID=spendlens
    secretEnv: ['GEMINI_API_KEY', 'GOOGLE_CLIENT_ID', 'GOOGLE_CLIENT_SECRET', 'SESSION_SECRET', 'CRON_SECRET']

availableSecrets:
  secretManager:
    - versionName: projects/spendlens-492305/secrets/GEMINI_API_KEY/versions/latest
      env: 'GEMINI_API_KEY'
    - versionName: projects/spendlens-492305/secrets/GOOGLE_CLIENT_ID/versions/latest
      env: 'GOOGLE_CLIENT_ID'
    - versionName: projects/spendlens-492305/secrets/GOOGLE_CLIENT_SECRET/versions/latest
      env: 'GOOGLE_CLIENT_SECRET'
    - versionName: projects/spendlens-492305/secrets/SESSION_SECRET/versions/latest
      env: 'SESSION_SECRET'
    - versionName: projects/spendlens-492305/secrets/CRON_SECRET/versions/latest
      env: 'CRON_SECRET'

images:
  - 'gcr.io/$PROJECT_ID/spendlens'
